// M7: .hst and .tab line plots (Plotly). Raw-file download links for the rest.

export function VisualizePanel() {
  return (
    <div className="mx-auto max-w-5xl px-6 py-10 text-foreground/70">
      Visualize panel (M7). .hst/.tab plots via /api/runs/:id/outputs/:name/series.
    </div>
  );
}
